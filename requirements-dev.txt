-r requirements.txt

# Testing
pytest==8.3.3
pytest-cov==5.0.0
requests==2.32.3   # tests/e2e talks to the Airflow and Metabase HTTP APIs

# Linting / formatting. These run in pre-commit AND in CI.
ruff==0.6.9
black==24.10.0

# Config / Dockerfile linting, not just Python
yamllint==1.35.1
